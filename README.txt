This is a high-level but very difficult integration package. It requires more than 4GB of memory (in fact, 3gb is enough, but it takes a long time to load), because the optimization is a lit
tle poor. This integration package contains technology, magic and adventure integration package.
这是一个高版本但是十分困难的整合包，需要4GB以上的内存(其实3GB就够了，就是加载有点久)，因为优化有点差。这个整合包包含科技，魔法，冒险的整合包。
 